apiVersion: apps/v1
kind: Deployment
metadata:
  name: lcb-web
  namespace: life-command
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lcb-web
  template:
    metadata:
      labels:
        app: lcb-web
    spec:
      containers:
        - name: web
          image: ghcr.io/madhuboyin/life-command-bar-poc/web:f1b4840
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: NEXT_PUBLIC_API_BASE_URL
              value: "https://api-lcb.contracttocozy.com/api"
            - name: API_BASE_URL
              value: "https://api-lcb.contracttocozy.com/api"
            - name: NEXTAUTH_URL
              value: "https://lcb.contracttocozy.com"
            - name: AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: lcb-secrets
                  key: AUTH_SECRET
            - name: AUTH_GOOGLE_ID
              valueFrom:
                secretKeyRef:
                  name: lcb-secrets
                  key: AUTH_GOOGLE_ID
            - name: AUTH_GOOGLE_SECRET
              valueFrom:
                secretKeyRef:
                  name: lcb-secrets
                  key: AUTH_GOOGLE_SECRET
            - name: API_AUTH_TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: lcb-secrets
                  key: API_AUTH_TOKEN_SECRET
                  optional: true
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: lcb-web
  namespace: life-command
spec:
  selector:
    app: lcb-web
  ports:
    - port: 3000
      targetPort: 3000
